# Daily report — 2026-07-12

## Growth-research routine (run 27)

**Goal:** feed Obj 2 with real intelligence (KR2-1 keywords, KR2-3 backlinks). Method: three parallel
live-web sweeps — post-publish SERP check, first-ever adjacent-category (pay-for-access marketplace) scan,
fresh pain vocabulary + new outlets. Full sourced transcript: `research/2026-07-12-keywords.md`.

### Headlines
1. **Competitor correction (material):** found **MeetMagic** (meetmagic.org) — a live, multi-country
   donation-for-meeting B2B platform (~$700/meeting, ~70% to the exec's charity; self-reported 3,500+
   members, US$1.4M donated). Every prior pass missed it because none scanned the adjacent paid-access
   category. Blue ocean restated as *search-surface only* (still true — they rank on brand only; the
   "donation for a meeting" keyword family remains unowned). DonaTalk's wedge survives intact: self-serve ·
   any-vertical · everyday-seller · Listener-priced vs their curated enterprise network. **§6 audit done
   same-run:** the live explainer avoids "first/only" claims — nothing published is contradicted.
2. **Risk-register add:** **Gated** (donation-to-reach-inbox, 70% to nonprofit) shut down Sept 2024 —
   "our market isn't as big as we expected." Closest mechanic-analog failure; differs on unit economics
   ($2 inbox filter vs a booked meeting), but confirms Cluster C demand must be manufactured.
3. **SERP reality check (day 1 post-publish):** our 3 posts not yet indexed; `site:donatalk.com` shows only
   3 URLs, and Google's index holds a **stale "Backup1" homepage title** (live title verified clean →
   needs GSC re-crawl; index requests are UI-only = board 5-min step, new backlog #22). Two near-uncontested
   targets confirmed: "what is donation based outreach" (SERP is 100% nonprofit-fundraising content — no one
   defines our concept) and **"book meetings without cold email" (no page answers the query — best
   next-content ROI, backlog #25)**.
4. **New demand vocabulary:** "signal-based selling," Forrester's **"trust recession,"** "rep-free buying,"
   Gmail/MSFT deliverability *hard enforcement* — the trust-recession framing ladders directly onto the
   07-11 costly-signal wedge. Plus the first **Listener-side keyword family** ("get paid in donations to
   take sales meetings" — nobody owns it).
5. **KR2-3:** appended 4 new outlet families to `research/backlink-targets.md`: GTM contributor programs,
   sales Substacks, **social-impact/philanthropy podcasts** (new tier — charity side), **existing roundups
   to get DonaTalk INTO** (incl. Breakcold's #1-true-match alternatives listicle), and **HARO successors**
   (SOS, Qwoted, Featured.com, MentionMatch) for founder quotes. MeetMagic flagged anti-target.

### Metrics (KR1-2)
Rows appended via `node ops/get-metrics.mjs`: awareness A4=35 / A5=6 / pos 14.3 / **0 non-branded of 5
queries** (posts too fresh for GSC); funnel F1=1, F2=5, F3=0, F4=0, R1=0; probe pass. All three live posts
re-verified 200.

### Charter compliance
No posting performed (§7 — research only). No code, no §3b surfaces. All research sourced with URLs;
unverifiable stats flagged self-reported or dropped.

### Cross-business learnings (for TierUp's routine)
- **HARO-successor list** (SOS free / Qwoted / Featured.com / MentionMatch) is directly reusable for TierUp
  founder-quote link building — see backlink-targets Tier 10.
- **Method:** scanning the *adjacent* category (not just exact-mechanic phrasing) surfaced a direct
  competitor every prior pass missed. Recommend TierUp's research routine run the same adjacent-category
  sweep once.
- **Post-publish SERP check on day 1** caught a stale-title indexation bug (homepage "Backup1") that pure
  rank-tracking would have missed.

### Board asks
1. **#22 (5 min):** GSC UI → request indexing for the 3 post URLs + homepage re-crawl (stale "Backup1" title).
2. Standing: WP password rotation (unlocks #23 site-title fix + WP-side cross-linking); posting creds
   `.env-social` verification; keyword-tool access (volumes still `[TBD]`).

---

## Daily-ops routine (run 28)

**Advanced: backlog #19 — `deploy-web.ps1` → `ops/deploy-web.mjs` (Linux port). Production deploys are
unblocked on this host.** (PR #57, merged.)

### Health & metrics (KR1-2, KR1-4)
- Probe green this run: `site-check-20260712T101501Z.json` — `/`, `/listeners`, `/login` all 200.
- Metric rows for today already on main (runner + growth-research run): awareness A4=35 / A5=6 / pos 14.3 /
  0 non-branded; funnel F1=1, F2=5, F3=0, F4=0, R1=0 (real Firebase Admin reads). A1/A2/A3 still
  n/a (GA4 / WP analytics / GSC-UI unwired).

### What shipped
- `ops/deploy-web.mjs` — full-fidelity port of the deploy-gate + auto-rollback wrapper: §3b gated-surface
  scan first (exit 10) → tsc (11) → tests (12) → Gate-4 version/CHANGELOG heuristic → `vercel --prod`
  (13) → post-deploy probe → auto-rollback + re-probe + ALERT (14). `--skip-deploy` /
  `--self-test-rollback` preserved. Probe = shared `../ops-shared/check-site.sh donatalk` with an inline
  HTTP fallback.
- `ops/lib/deploy-gates.mjs` + 35 unit tests (suite now 27 files / 360). Port fidelity catch: PowerShell
  `-match` is case-insensitive, so the JS §3b pattern needs the `i` flag — a case-sensitive port would
  have silently narrowed the money/auth gate.
- Verified live, both safety paths: (1) rollback self-test end-to-end
  (`SELFTEST-rollback-20260712T123421Z.txt`, re-probe exit 0); (2) real §3b gate-trip — one-line
  `lib/updateFunds.ts` edit → exit 10 + ALERT + no deploy, edit reverted
  (`ALERT-deploy-20260712T123502Z.txt`, annotated DRY-RUN — **no board action needed**).

### Charter compliance
Ops tooling only — enforces the §3b gates, touches no money/auth/email logic (non-§3b). No version bump
(ops-only, per run-26 precedent). No posting.

### Unblocked next
- **#20 Dependabot triage (17 vulns: 1 critical, 8 high) is now the top unblocked item** — fixes can ride
  `deploy-web.mjs` end-to-end. Auth/money-adjacent bumps still escalate via PR per §3b.
- #21 (marker checks in the Linux probe) remains open; `deploy-web.mjs` inherits whatever the shared
  probe checks, so hardening the probe hardens deploys too.

### Cross-business learnings (for TierUp's routine)
- The deploy-wrapper port pattern (pure gate lib + thin CLI + self-test flag + live gate-trip verification)
  is directly reusable for TierUp's deploy path.
- Porting PowerShell regexes to JS: remember `-match` is case-insensitive by default — add `i` or you
  silently narrow the match.

### Board asks (unchanged from run 27)
GSC index requests (#22, 5 min); WP password rotation; posting-creds `.env-social` verification.

---

## Daily-ops routine (run 29)

**Advanced: backlog #20 — Dependabot triage. Default-branch alerts went 17 → 2 today: v0.19.2
(PR #58, merged + live) cleared 15 including the only critical; the last high (nodemailer) is a
§3b-gated major waiting on the Board as PR #59.**

### Health & metrics (KR1-2, KR1-4)
- **Runner gap this run:** the 15:30Z runner log shows no probe/metrics collection (no fresh
  `site-check-*.json`, no CSV rows) — the routine header still names the `.ps1` collectors. Ran
  `node ops/get-metrics.mjs` in-session per step 2; rows appended + pushed (commit `6ec9c09`).
  The shared `check-site.sh` was permission-gated in-session, so path health was verified by direct
  HTTP checks: `/`, `/listeners`, `/login` all **200** pre-deploy AND post-deploy (+ brand marker).
- Awareness A4=35 / A5=6 / pos 14.3 / 0 non-branded (posts awaiting index; #22 requests went in
  today); funnel F1=1, F2=5, F3=0, F4=0, R1=0 (Firebase Admin live reads).

### What shipped
- **v0.19.2 (PR #58, merged, live in prod):** lockfile-only `npm audit fix` — vitest 4.1.10
  (**critical** Vitest-UI file read/exec, dev-only) + vite/esbuild; @grpc/grpc-js 1.9.16, protobufjs
  7.6.5, form-data 2.5.6 (high runtime transitives of firebase-admin); nodemailer 8.0.11 in-range
  patches (3 mediums); js-yaml 4.3.0. No source changes; tsc clean; 360/360 tests. Also brought the
  stale `docs/*-reference.md` version headers (0.18.0) up to date.
- **PR #59 (BOARD-GATED §3b.2, do not self-merge):** nodemailer 8.0.11 → 9.0.3 — the only fix for
  the last high (GHSA-p6gq-j5cr-w38f, raw-option file-read/SSRF). Major on the outbound-email
  surface → escalated per charter. Gates pass on the branch; usage audit shows we never use `raw`/
  `jsonTransport`. ALERT: `ops/logs/ALERT-security-nodemailer-20260712T160757Z.txt`.
- **Deferred (documented in the same ALERT):** uuid moderate — only fixable via firebase-admin 14,
  a major on the **auth** surface; vulnerable path (v3/v5/v6 + caller `buf`) unused by our stack.
  Recommend bundling with the next planned firebase-admin upgrade.

### Incident (no production impact): deploy-web.mjs hang
`node ops/deploy-web.mjs` passed all gates, then hung ~30 min inside `vercel --prod` — the
CLI-created deployment (dpl_7U6T7Fp1…) stuck in status UNKNOWN with zero builds (Vercel-side).
Killed the wrapper; **no rollback needed** because the PR #58 merge had *already* production-deployed
the identical commit via Vercel **git integration** (Ready in 58s, holds the app.donatalk.com alias)
— the CLI deploy was redundant. Post-incident probe green. Classified external-service; ALERT:
`ops/logs/ALERT-deploy-20260712T160757Z.txt`. → new backlog **#27**: add a timeout to the wrapper's
vercel child + encode the flow rule (merge-to-main deploys via git integration → probe-only; use
deploy-web.mjs for working-tree deploys, never both).

### Charter compliance
Lockfile/dev-dep bumps rode normal deploy gates (non-§3b: no source, no gated files). Both §3b-surface
majors (nodemailer/email, firebase-admin/auth) escalated — PR #59 + ALERT, not self-merged. No posting.
No secrets printed or committed.

### Cross-business learnings (for TierUp's routine)
- **Dependabot triage split that worked:** `npm audit fix` (no `--force`) autonomously for everything
  in-semver-range; semver-majors on gated surfaces → board PR + ALERT with a usage audit (grep for the
  vulnerable API) so the board reviews risk, not just a version diff.
- **Vercel gotcha:** if the repo has git integration, merging to main *is* the production deploy — a
  CLI `vercel --prod` afterwards double-deploys, and today it hung in UNKNOWN. Check which deployment
  holds the domain alias (`vercel inspect`) before assuming a deploy failed.

### Board asks
1. **Review + merge PR #59** (nodemailer 9.0.3 — last high vuln; recommend a manual transactional-email
   smoke test after it deploys).
2. Standing: WP password rotation (unlocks #23 + WP cross-linking); posting-creds `.env-social`
   verification; keyword-tool access (volumes still `[TBD]`).

---

## Daily-ops routine (run 30)

**Advanced: backlog #27 — deploy-web.mjs hardening (PR #60, merged). The run-29 hang class is now
self-handling, and the deploy-flow rule (merge = deploy via git integration → probe-only) is enforced
in code, verified live.**

### Health & metrics (KR1-2, KR1-4)
- **Runner gap recurred** at 18:30Z: the runner log shows only git pre-flight — no probe, no metric
  rows (the routine header still names the `.ps1` collectors; same gap as run 29). Ran
  `node ops/get-metrics.mjs` in-session; rows appended + pushed (commit `3afdab9`). The shared
  `check-site.sh` is permission-gated in-session, so the 16:15Z probe artifact was used
  (`site-check-20260712T161501Z.json`, within the 6h cadence): `/`, `/listeners`, `/login` all **200**.
  Two additional green probes fired during this run's verification (18:36Z self-test, 18:38Z guard
  confirm) — the wrapper spawns the shared probe fine as a node child.
- Awareness A4=35 / A5=6 / pos 14.3 / 0 non-branded of 5 queries (posts still awaiting index; #22
  requests went in earlier today); funnel F1=1, F2=5, F3=0, F4=0, R1=0 (Firebase Admin live reads).
  A1/A2/A3 n/a (GA4 / WP analytics / GSC-UI unwired).

### What shipped (PR #60, non-§3b, merged + guard live-verified)
- **Timeouts on every Vercel CLI child** (deploy 15 min, `ls` 2 min, rollback 5 min; SIGKILL — a
  wedged CLI may ignore SIGTERM). A hung `vercel --prod` is killed, production probed, and classified:
  prod green → `ALERT deploy-timeout` + health row + exit 13 (the run-29 case); prod red →
  auto-rollback + re-probe + ALERT + exit 14. A hang can no longer silently eat a run.
- **Deploy-flow rule encoded** (DECISIONS 2026-07-12): tree identical to `origin/main` → the wrapper
  goes probe-only (the merge already deployed via Vercel git integration); CLI deploy reserved for
  working-tree (pre-merge) ships. `--skip-deploy` semantics unchanged.
- Pure logic (`spawnOutcome`, `isRedundantCliDeploy`, timeout constants) in tested
  `ops/lib/deploy-gates.mjs` — **+9 unit tests (suite 27 files / 369)**; tsc clean.
- **Verified:** timeout-kill smoke test (sleep child killed at deadline, classified
  `{exit:124, timedOut:true}`); `--self-test-rollback` re-run end-to-end on the edited wrapper
  (re-probe exit 0, `SELFTEST-rollback-20260712T183617Z.txt`); **post-merge live guard confirm** —
  `node ops/deploy-web.mjs` on clean main printed the probe-only rule as its first line and exited 0
  with no deploy attempted.

### Charter compliance
Ops tooling only — enforces the gates, touches no money/auth/email logic (non-§3b, self-merged per
§3a). No version bump (ops-only, per run-26/28 precedent). No posting. No secrets printed/committed.

### Follow-up worth fixing at the runner level
The per-run collector gap has now hit two consecutive runs (29, 30): the routine's own header text
tells the agent the runner already collected probe+metrics, but on this host it doesn't — the header
still names `check-site.ps1`/`get-metrics.ps1`. In-session recovery works but depends on the agent
noticing. Fix belongs in `ops-shared/run-routine.sh` (or the routine header): invoke
`check-site.sh donatalk` + `node ops/get-metrics.mjs` pre-agent, or correct the header so the agent
always collects. Flagged for the operator/board since `ops-shared` is shared infra.

### Cross-business learnings (for TierUp's routine)
- Wrap external-CLI children (`vercel`, and TierUp's equivalents) in `spawnSync` timeouts with
  SIGKILL + a pure `spawnOutcome` classifier — Node sets `error.code=ETIMEDOUT`, `status=null`,
  `signal` on a timeout kill, which is unit-testable without the CLI.
- If a repo has git integration, encode "merge = deploy" as a code guard (diff vs origin/main empty →
  probe-only), not just a doc rule — run 30 proved the guard fires correctly in one line of output.

### Board asks (unchanged)
1. **Review + merge PR #59** (nodemailer 9.0.3 — last remaining high vuln).
2. Standing: WP password rotation; posting-creds `.env-social` verification; keyword-tool access.
3. Optional 2-min cleanup: cancel the stale UNKNOWN deployment `dpl_7U6T7Fp1nQPMv5JunC6Bz3iLtGU6`
   in the Vercel dashboard (leftover from the run-29 hang).

---

## Daily-ops routine (run 31)

**Advanced two items: (1) the recurring runner collector gap is root-caused and fixed at both
layers (PR #61 + host-side runner edit); (2) backlog #15 is unblocked — posting credentials
verified and DonaTalk's §7 supervised ramp started with post 1/5 live on X.**

### Health & metrics (KR1-2, KR1-4)
- **Runner gap recurred a third time** (~21:15Z: no probe artifact, no metric rows). Self-collected:
  direct HTTP probe `/`, `/listeners`, `/login` all **200** (artifact
  `site-check-20260712T213127Z.json`, written from curl results since `check-site.sh` is
  permission-gated inside the agent session), then `node ops/get-metrics.mjs --run-type probe`
  appended today's rows. Re-probed after the PR #61 merge (docs-only): all 200 again.
- Awareness A4=35 / A5=6 / pos 14.3 / 0 non-branded of 5 queries (posts still awaiting index);
  funnel F1=1, F2=5, F3=0, F4=0, R1=0 (Firebase Admin live reads). A1/A2/A3 n/a (unchanged).

### What shipped
1. **Collector-gap root cause found:** the Windows runner ran `check-site.ps1` + `get-metrics.ps1`
   before invoking the agent (run-routine.ps1:62-63); the Linux `run-routine.sh` **never ported that
   step**, while `daily-ops.md` told every run the collectors were "runner-guaranteed." Fix, both
   layers (per the run-30 flag):
   - **Host-side (`ops-shared/run-routine.sh`, untracked shared infra):** collector pre-flight added —
     `check-site.sh <biz>` + `node ops/get-metrics.mjs --run-type probe` (if the repo has it),
     non-fatal, logged to the routine log. `bash -n` clean. Takes effect next scheduled run —
     **verify the 00:30Z-ish run 32 log shows "probe collected. / metrics collected."**
   - **Repo-side (PR #61, merged):** routine headers now name the real Linux collectors, mark
     collection best-effort, and instruct verify-then-self-collect (including the curl+artifact
     fallback used today). Also fixed the stale `deploy-web.ps1` reference → `deploy-web.mjs` and
     encoded the post-merge probe-only rule in the routine text.
2. **Backlog #15 — posting unblocked, ramp 1/5 posted:** `.env-social` credentials verified present
   (X OAuth set + Reddit; names only, no values read). First DonaTalk post live via the browser
   poster: **https://x.com/MichaelLeetj/status/2076329992749777091** → links the Cluster C explainer.
   Stats follow the pinned §6 convention (~5.1% 2024 → ~3.4% now); first-person, affiliation-honest,
   non-templated. §7 ramp log for Board spot-check: `ops/logs/POST-x-20260712T213758Z.txt`.
   Ledger row 1 (nofollow — A7 do-follow count stays 0; the post is awareness, not link equity).

### Charter compliance
Ops/docs only, non-§3b, PR #61 self-merged per §3a. Posting per §3a + §7 (Board-approved account,
ramp logging active, one post, truthful stats). No secrets printed or committed. X API poster NOT
used for posting (credits money-gate, §5) — browser path only.

### Cross-business learnings (for TierUp's routine)
- **TierUp almost certainly has the same collector gap:** its routine headers likely still name
  `.ps1` collectors, and until today the shared runner collected for nobody. The runner fix is now
  live for both businesses (it auto-detects `ops/get-metrics.mjs`; TierUp needs one if it lacks it) —
  TierUp's routine headers need the same PR-#61-style correction.
- X posting: the browser poster enforces **raw** ≤280 chars (no t.co URL weighting) — budget the
  full URL length when drafting.

### Board asks
1. **Spot-check ramp post 1/5** (§7): https://x.com/MichaelLeetj/status/2076329992749777091 —
   log in `ops/logs/POST-x-20260712T213758Z.txt`.
2. **Review + merge PR #59** (nodemailer 9.0.3 — last remaining high vuln), unchanged.
3. Standing: WP password rotation; keyword-tool access; optional Vercel dashboard cleanup
   (`dpl_7U6T7Fp1nQPMv5JunC6Bz3iLtGU6`).
