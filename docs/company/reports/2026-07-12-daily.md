# Daily report — 2026-07-12

## Growth-research routine (run 27)

**Goal:** feed Obj 2 with real intelligence (KR2-1 keywords, KR2-3 backlinks). Method: three parallel
live-web sweeps — post-publish SERP check, first-ever adjacent-category (pay-for-access marketplace) scan,
fresh pain vocabulary + new outlets. Full sourced transcript: `research/2026-07-12-keywords.md`.

### Headlines
1. **Competitor correction (material):** found **MeetMagic** (meetmagic.org) — a live, multi-country
   donation-for-meeting B2B platform (~$700/meeting, ~70% to the exec's charity; self-reported 3,500+
   members, US$1.4M donated). Every prior pass missed it because none scanned the adjacent paid-access
   category. Blue ocean restated as *search-surface only* (still true — they rank on brand only; the
   "donation for a meeting" keyword family remains unowned). DonaTalk's wedge survives intact: self-serve ·
   any-vertical · everyday-seller · Listener-priced vs their curated enterprise network. **§6 audit done
   same-run:** the live explainer avoids "first/only" claims — nothing published is contradicted.
2. **Risk-register add:** **Gated** (donation-to-reach-inbox, 70% to nonprofit) shut down Sept 2024 —
   "our market isn't as big as we expected." Closest mechanic-analog failure; differs on unit economics
   ($2 inbox filter vs a booked meeting), but confirms Cluster C demand must be manufactured.
3. **SERP reality check (day 1 post-publish):** our 3 posts not yet indexed; `site:donatalk.com` shows only
   3 URLs, and Google's index holds a **stale "Backup1" homepage title** (live title verified clean →
   needs GSC re-crawl; index requests are UI-only = board 5-min step, new backlog #22). Two near-uncontested
   targets confirmed: "what is donation based outreach" (SERP is 100% nonprofit-fundraising content — no one
   defines our concept) and **"book meetings without cold email" (no page answers the query — best
   next-content ROI, backlog #25)**.
4. **New demand vocabulary:** "signal-based selling," Forrester's **"trust recession,"** "rep-free buying,"
   Gmail/MSFT deliverability *hard enforcement* — the trust-recession framing ladders directly onto the
   07-11 costly-signal wedge. Plus the first **Listener-side keyword family** ("get paid in donations to
   take sales meetings" — nobody owns it).
5. **KR2-3:** appended 4 new outlet families to `research/backlink-targets.md`: GTM contributor programs,
   sales Substacks, **social-impact/philanthropy podcasts** (new tier — charity side), **existing roundups
   to get DonaTalk INTO** (incl. Breakcold's #1-true-match alternatives listicle), and **HARO successors**
   (SOS, Qwoted, Featured.com, MentionMatch) for founder quotes. MeetMagic flagged anti-target.

### Metrics (KR1-2)
Rows appended via `node ops/get-metrics.mjs`: awareness A4=35 / A5=6 / pos 14.3 / **0 non-branded of 5
queries** (posts too fresh for GSC); funnel F1=1, F2=5, F3=0, F4=0, R1=0; probe pass. All three live posts
re-verified 200.

### Charter compliance
No posting performed (§7 — research only). No code, no §3b surfaces. All research sourced with URLs;
unverifiable stats flagged self-reported or dropped.

### Cross-business learnings (for TierUp's routine)
- **HARO-successor list** (SOS free / Qwoted / Featured.com / MentionMatch) is directly reusable for TierUp
  founder-quote link building — see backlink-targets Tier 10.
- **Method:** scanning the *adjacent* category (not just exact-mechanic phrasing) surfaced a direct
  competitor every prior pass missed. Recommend TierUp's research routine run the same adjacent-category
  sweep once.
- **Post-publish SERP check on day 1** caught a stale-title indexation bug (homepage "Backup1") that pure
  rank-tracking would have missed.

### Board asks
1. **#22 (5 min):** GSC UI → request indexing for the 3 post URLs + homepage re-crawl (stale "Backup1" title).
2. Standing: WP password rotation (unlocks #23 site-title fix + WP-side cross-linking); posting creds
   `.env-social` verification; keyword-tool access (volumes still `[TBD]`).

---

## Daily-ops routine (run 28)

**Advanced: backlog #19 — `deploy-web.ps1` → `ops/deploy-web.mjs` (Linux port). Production deploys are
unblocked on this host.** (PR #57, merged.)

### Health & metrics (KR1-2, KR1-4)
- Probe green this run: `site-check-20260712T101501Z.json` — `/`, `/listeners`, `/login` all 200.
- Metric rows for today already on main (runner + growth-research run): awareness A4=35 / A5=6 / pos 14.3 /
  0 non-branded; funnel F1=1, F2=5, F3=0, F4=0, R1=0 (real Firebase Admin reads). A1/A2/A3 still
  n/a (GA4 / WP analytics / GSC-UI unwired).

### What shipped
- `ops/deploy-web.mjs` — full-fidelity port of the deploy-gate + auto-rollback wrapper: §3b gated-surface
  scan first (exit 10) → tsc (11) → tests (12) → Gate-4 version/CHANGELOG heuristic → `vercel --prod`
  (13) → post-deploy probe → auto-rollback + re-probe + ALERT (14). `--skip-deploy` /
  `--self-test-rollback` preserved. Probe = shared `../ops-shared/check-site.sh donatalk` with an inline
  HTTP fallback.
- `ops/lib/deploy-gates.mjs` + 35 unit tests (suite now 27 files / 360). Port fidelity catch: PowerShell
  `-match` is case-insensitive, so the JS §3b pattern needs the `i` flag — a case-sensitive port would
  have silently narrowed the money/auth gate.
- Verified live, both safety paths: (1) rollback self-test end-to-end
  (`SELFTEST-rollback-20260712T123421Z.txt`, re-probe exit 0); (2) real §3b gate-trip — one-line
  `lib/updateFunds.ts` edit → exit 10 + ALERT + no deploy, edit reverted
  (`ALERT-deploy-20260712T123502Z.txt`, annotated DRY-RUN — **no board action needed**).

### Charter compliance
Ops tooling only — enforces the §3b gates, touches no money/auth/email logic (non-§3b). No version bump
(ops-only, per run-26 precedent). No posting.

### Unblocked next
- **#20 Dependabot triage (17 vulns: 1 critical, 8 high) is now the top unblocked item** — fixes can ride
  `deploy-web.mjs` end-to-end. Auth/money-adjacent bumps still escalate via PR per §3b.
- #21 (marker checks in the Linux probe) remains open; `deploy-web.mjs` inherits whatever the shared
  probe checks, so hardening the probe hardens deploys too.

### Cross-business learnings (for TierUp's routine)
- The deploy-wrapper port pattern (pure gate lib + thin CLI + self-test flag + live gate-trip verification)
  is directly reusable for TierUp's deploy path.
- Porting PowerShell regexes to JS: remember `-match` is case-insensitive by default — add `i` or you
  silently narrow the match.

### Board asks (unchanged from run 27)
GSC index requests (#22, 5 min); WP password rotation; posting-creds `.env-social` verification.
