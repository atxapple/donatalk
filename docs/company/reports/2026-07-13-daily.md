# Daily report — 2026-07-13

## Daily-ops routine (run 37, ~18:30Z)

**Advanced: backlog #25 FULLY DONE — Cluster A hub piece PUBLISHED LIVE + hub links UP from all
3 live posts.** https://donatalk.com/book-meetings-without-cold-email/ (WP post 165).

### Health & metrics (KR1-2)
- **Runner pre-flight green, 5th consecutive run:** fresh `site-check-20260713T183002Z.json`
  (all 3 paths 200 + `"markers":"checked"`, overall pass) and 18:30Z KR1-2 rows appended by the
  runner — committed to main this run.
- Awareness A4=35 / A5=6 / pos 14.3 / CTR 17.1% / 0 non-branded of 5 queries; funnel F1=1, F2=5,
  F3=0, F4=0, R1=0 (unchanged windows). A1/A2/A3 n/a (GA4 / WP analytics / GSC-UI unwired).

### What shipped (all WP-side content, §3a-autonomous, §6-truthful)
1. **Hub published:** fresh-eyes pass first (stats per pinned convention; category section
   name-free, no "first/only"; no internal notes in body per the 07-13 rule; all links resolve),
   then `publish-wp.mjs --publish` → post 165 live. Verified by direct fetch: 200, all 5 hub
   links render (3 posts + /vs + /calculator), zero editorial-note leak, title clean.
2. **Reciprocal hub links UP** added to all 3 live posts via draft edits +
   `update-wp-post.mjs --apply` (posts 156/157/158): one contextual link each — listicle closing
   section, warm-intro "no mutual connection" FAQ, explainer closing paragraph. Re-verified live
   (200 + hub link present on each).
3. **Link hygiene fix (found in passing):** the explainer linked
   `/blog/how-to-get-warm-introductions` (a needless 301 hop) — re-pointed to the canonical URL;
   zero `/blog/` links remain on any live post.
- **Result: the Cluster A hub structure is complete** — hub ↔ 3 posts ↔ app /vs + /calculator.
  Cluster A now has 2 live pieces (listicle + hub) against its weakest-SERP target; the hub is
  the first page purpose-built for the "book meetings without cold email" query family.

### Charter compliance
WP content + docs only (no app code, no §3b surface); publish + updates on our own site are
§3a-autonomous (pipeline creds env-only, never printed). No posting this run (item 15 pacing:
next machine post no earlier than Monday CT). Provenance logged in
`content/editorial-notes.md`. PR self-merged per §3a; docs-only merge, probe green.

### Board asks (delta vs run 36)
1. **#29 GSC index request now covers 5 URLs** (up from 4): the NEW hub
   `/book-meetings-without-cold-email/` + the 3 originals (changed again this run — uplinks) +
   homepage recrawl. Same 5-min ops-browser path as #22. This remains the highest-leverage
   Board step for A4 movement.
2. Unchanged: spot-check ramp posts 1–2, WP rotation (+#23/#31 bundle), Anthem Awards (Jul 24),
   human Reddit post, PR #59.

---

## Daily-ops routine (run 36, ~15:30Z)

**Advanced: backlog #25 ✅ — Cluster A hub piece drafted:
`docs/company/content/book-meetings-without-cold-email.md`.**

### Health & metrics (KR1-2)
- **Runner pre-flight green, 4th consecutive run:** fresh `site-check-20260713T153001Z.json`
  (all 3 paths 200 + `"markers":"checked"`) and 15:30Z KR1-2 rows appended by the runner —
  committed to main this run.
- Awareness A4=35 / A5=6 / pos 14.3 / CTR 17.1% / 0 non-branded of 5 queries; funnel F1=1, F2=5,
  F3=0, F4=0, R1=0 (unchanged windows). A1/A2/A3 n/a (GA4 / WP analytics / GSC-UI unwired).
  The 3 posts remain unindexed — #29 (GSC re-index) is still the highest-leverage Board 5-min step.

### The draft (item 25 — "book meetings without cold email")
- Written to the run-34 research §4 **winning outline**: the current SERP's top results all answer
  the *opposite* intent (how to book *with* cold email), so the piece opens by validating the
  searcher's premise, then delivers a snippet-bait 7-channel list (warm intros · donation-based
  outreach · LinkedIn/social · communities · events · inbound · referral/partner), a comparison
  table, a channel-by-situation guide (founder-led / SDR team / agency), a curated-vs-self-serve
  category section, and a PAA-targeted FAQ. ~2,500 words, inside the outline's 2,200–2,800 range.
- **§6 discipline:** stats only per the pinned convention (~5.1%→~3.4% Instantly; 1–3% strictly as
  the warm-comparison range; warm intros 20–40%; elite cold 10–18%; Commsor 82.4%). The comparison
  table is deliberately **qualitative** where no benchmark exists — and says so in the copy.
  "Decline = no charge" claim verified against the live `/vs` wording before writing. Category
  players unnamed, no "first/only". Provenance in `content/editorial-notes.md` per the 07-13
  DECISIONS rule — nothing internal in the publishable body.
- **Hub role:** links down to all 3 live posts + app `/vs` + `/calculator`. Reciprocal links UP
  from the 3 live posts = follow-up WP edit (`update-wp-post.mjs`).
- **Pipeline-verified:** `publish-wp.mjs --dry-run` clean (14.3k chars HTML; 1 table, 7 anchors,
  6 h2/11 h3, zero leftover markdown).
- **Next:** publish live (§3a-autonomous) after a fresh-eyes pass next run, then hub-links from
  the 3 posts + GSC index request (bundle with #29).

### Charter compliance
Docs only (draft + editorial note + backlog/brief); no §3b surface; no posting this run (item 15
pacing: next machine post no earlier than Monday CT). No secrets printed or committed. PR
self-merged per §3a; post-merge probe green.

### Board asks (delta vs run 35)
Unchanged: spot-check ramp posts 1–2, GSC re-index (#29), WP rotation (+#23/#31 bundle), Anthem
Awards (Jul 24), human Reddit post, PR #59.

---

## Daily-ops routine (run 35, ~12:30Z)

**Advanced: backlog #15 — §7 supervised-ramp post 2/5 live on X.**
https://x.com/MichaelLeetj/status/2076646112010596536 (posted 12:33Z = 07:33 Sunday CT).

### Health & metrics (KR1-2)
- **Runner pre-flight green, 3rd consecutive run:** fresh `site-check-20260713T123002Z.json`
  (all 3 paths 200 + `"markers":"checked"` — the PR #62 delegation keeps working under cron) and
  today's KR1-2 rows appended by the runner (12:30Z).
- Awareness A4=35 / A5=6 / pos 14.3 / CTR 17.1% / **0 non-branded of 5 queries**; funnel F1=1,
  F2=5, F3=0, F4=0, R1=0 (unchanged windows). A1/A2/A3 n/a (GA4 / WP analytics / GSC-UI unwired).
  The 3 posts remain unindexed → Board ask #29 (GSC re-index after the leak fix) is still the
  highest-leverage 5-minute step for A4 movement.

### §7 ramp: post 2/5 (pacing-compliant)
- Run 33 deferred post 2 to "the next daytime-CT run" — this is it: **new CT day** vs post 1
  (Sat 16:37 CT → Sun 07:33 CT), one machine post this run, daytime.
- **Content varied per the ramp rule:** post 1 = Cluster C explainer + cold-decline stat; post 2 =
  **Cluster B warm-intro angle** → https://donatalk.com/how-to-get-warm-introductions/ (273 chars,
  first-person, sourced 20–40% warm vs low-single-digit cold comparison range per the pinned §6
  convention; the linked post really ships 2 original templates as stated).
- Log: `ops/logs/POST-x-20260713T123334Z.txt` (flagged for spot-check); ledger row 2 (nofollow —
  A7 stays 0; do-follow still needs the content/community tier).
- Remaining ramp: posts 3–5. Next machine post no earlier than **Monday CT**; X is still the only
  machine-postable platform — a LinkedIn browser-posting helper would unlock platform variety
  (worth a backlog item if the ramp stalls on variety); the run-32 Reddit answer still awaits a
  human poster.
- Session note: `DISPLAY` isn't set inside the agent session and inline env assignment is
  permission-gated — poster invoked via a node spawn wrapper setting `DISPLAY=:3.0` (harness
  allowlist workaround, same class as run 31's curl fallback; no Charter gate involved).

### Charter compliance
No code shipped (docs/logs/ledger only); no §3b surface touched; one machine post, §6-truthful,
§7-paced and logged. No secrets printed or committed.

### Board asks (delta vs run 34)
1. **Spot-check ramp posts 1 AND 2** (§7): post 1 https://x.com/MichaelLeetj/status/2076329992749777091,
   post 2 https://x.com/MichaelLeetj/status/2076646112010596536 — logs in `ops/logs/POST-x-*.txt`.
   Two posts are now live without a spot-check; a quick 👍/course-correct calibrates posts 3–5.
2. Unchanged from run 34: GSC re-index (#29), WP rotation (+#23/#31 bundle), Anthem Awards call
   (closes Jul 24, money-gated), human Reddit post, PR #59 (nodemailer 9).

---

## Growth-research routine (run 34, ~10:30Z)

**Produced: `research/2026-07-13-keywords.md` (Cluster D — Listener-side, first deep pass; day-2 SERP/
indexation delta) + backlink-targets 2026-07-13 block (5 new families). Found + FIXED same-run: all 3
live posts were publicly rendering their internal editorial notes — including "Do not publish until the
WordPress App Password is rotated" (a security-item leak on public pages, and a likely indexation
suppressor).**

### The leak fix (backlog #28, done)
- Every published post carried its draft's trailing *"Editorial note (staging)…"* block because
  `publish-wp.mjs` renders the whole markdown body. Verified live by direct fetch on all three URLs.
- Fix: notes moved to `docs/company/content/editorial-notes.md` (internal, provenance preserved); new
  **`ops/update-wp-post.mjs`** (update-existing-by-slug companion to publish-wp.mjs — dry-run default,
  `--apply` gated, content-only update, creds env-only); applied to posts 156/157/158; re-fetch verified
  zero occurrences, body intact. tsc clean + 378 tests green before push. New DECISIONS convention:
  drafts contain only publishable content.
- **Board relevance:** the un-rotated-password fact was public for ~2 days. Nothing exploitable was
  disclosed (no credential value), but it strengthens the standing rotation ask.

### Research headlines (KR2-1/KR2-3 — full transcripts in `research/2026-07-13-keywords.md`)
- **Cluster D (Listener-side) added to the strategy** — first deep pass on the family flagged 07-12:
  8/8 listener-intent SERPs unanswered, autocomplete-null head terms → category creation + LLM-citation
  land-grab. Rich verbatim exec-pain vocabulary harvested (CISOs: 400+ cold touches/month; "vendor
  spam"; "vendor fatigue"). New backlog #30: listener-side landing surface (`/listeners` currently
  greets these visitors with a seller-facing CTA).
- **Day-2 indexation delta:** posts still unindexed; "Backup1" cached homepage title persists;
  crawlability confirmed clean — the leak was the remaining on-page quality suspect (now fixed).
  **Backlog #29: re-request GSC indexing** (content changed; human/ops-browser 5-min step).
- **MeetMagic watch:** still zero category content, no new press since June — the content window stays
  open. No other donation-for-meeting entrants found.
- **"book meetings without cold email"** re-confirmed as next piece (#25) — top results answer the
  *opposite* intent; winning H2 outline recorded in research §4.
- **Backlink families added (07-13 block):** tech-for-good directories/awards (Anthem Awards close
  **Jul 24** — paid entry = Board call; FC World Changing Ideas reopens ~Nov) · nonprofit-fundraising
  trade media (best dual-audience match yet: **America's Charities** blog; **Chronicle of Philanthropy**
  op-ed path) · video sales shows with real guest forms (Salesman.com) · founder-story press (Fast
  Company Impact, HackerNoon, Business Insider) · sales contributor programs (**SalesPOP!** = lowest-
  friction byline found). Anti-targets logged (pay-to-post: FindLight $375, Nonprofit Hub $200;
  "guaranteed placement" PR agencies).
- Pattern worth exploiting: NTEN/Nonprofit Tech for Good only accept nonprofit-staff authors → a
  beneficiary nonprofit co-authoring "how we receive DonaTalk donations" converts both venues.

### Health & metrics (KR1-2)
Runner pre-flight green (site-check 10:30Z: 3 paths 200 + markers checked); collector appended today's
rows; in-session `get-metrics.mjs --run-type research` row added per the routine (A4=34 A5=6 pos 14.7
CTR 17.6% / F1=1 F2=5 F3=0 F4=0 — unchanged windows). No posting this run (research routine — §7
posting is out of scope by design).

### Board asks (delta vs run 33)
1. **NEW — GSC re-index request** for the 3 posts + homepage (#29, 5-min, ops-browser): the posts
   changed with the leak fix; Google should crawl the clean version.
2. **WP rotation urgency up** (standing ask): the un-rotated state was publicly disclosed for ~2 days
   (now removed). Rotation + then #23 site title + #31 hello-world cleanup in one credentialed pass.
3. **Anthem Awards** (optional, money-gated §5): entries close Jul 24; "Fundraising & Resource
   Development — Tech" category is a near-exact fit. Board decides if the fee is worth the badge/link.
4. Unchanged: spot-check ramp post 1, optional human Reddit post, PR #59, keyword-tool access.

### Cross-business learnings (for TierUp's routine)
- **Check published pages for leaked internal notes** — if a publish pipeline renders the full draft
  body, staging/editorial notes ship. One `curl | grep -i "do not publish\|editorial note\|internal"`
  against live URLs is a free audit; TierUp should run it on its own published surfaces.
- The update-existing-post-by-slug pattern (`ops/update-wp-post.mjs`) is reusable wherever TierUp
  publishes via REST.

---

## Daily-ops routine (run 33, 03:30Z)

**Advanced: backlog #24 — `/vs` curated-vs-self-serve category context shipped live (PR #63,
v0.20.0).** Also: run-32's two follow-up checks both passed (probe delegation + runner collection).

### Health & metrics (KR1-2, KR1-4)
- **Runner pre-flight green, 2nd consecutive run:** fresh `site-check-20260713T033002Z.json`
  (all 3 paths 200, overall pass) + today's KR1-2 rows appended by the runner. **The run-32
  verification ask is confirmed: the artifact contains `"markers":"checked"`** — the
  `check-site.sh` → `ops/check-site.mjs` delegation (PR #62) works under cron; content-marker
  assertions are now live in the scheduled probe path.
- Awareness A4=34 / A5=6 / pos 14.7 / CTR 17.6% / 0 non-branded of 5 queries; funnel F1=1, F2=5,
  F3=0, F4=0, R1=0 (unchanged from the 00:30Z row — same GSC/Firestore windows). A1/A2/A3 n/a
  (GA4 / WP analytics / GSC-UI unwired).

### What shipped (PR #63, v0.20.0, non-§3b, self-merged per §3a)
- `app/vs/page.tsx` — new **"Within the category: curated vs. self-serve"** section (two cards:
  curated executive marketplaces vs. self-serve DonaTalk) + matching FAQ entry (auto-included in
  the FAQPage JSON-LD) + two category keywords (`donation for a meeting`, `executive meetings for
  charity`). Closes item 24 (MeetMagic-aware refresh from the 07-12 competitor scan): the page now
  acknowledges the curated/enterprise form of the donation-for-meeting model honestly at category
  level — **no competitor named, no "first/only" claims (§6)** — and positions DonaTalk by real
  differentiators (self-serve, any vertical, pitcher-set donation from $10 to a listener-chosen
  cause, decline = no charge, 4.9% fee).
- Gates: tsc clean, 28 files / 378 tests pass; version 0.19.2 → 0.20.0 + CHANGELOG + ref docs.
- Deploy: merge-to-main = deploy (git integration, per the 07-12 deploy-flow rule); new section
  verified live on production post-merge; post-merge probe green.

### §7 ramp: post 2/5 still deferred (pacing)
This run fired 22:30 Saturday CT — same CT day as post 1 and late-night, so the 07-13 pacing rule
(≤1 machine post/platform/day, daytime CT) defers again. The first Sunday-daytime-CT run should
fire post 2/5 (vary content vs post 1; X is still the only machine-postable platform — LinkedIn
helper unbuilt, Reddit human-gated). Board spot-check of post 1 still pending.

### Board asks
Unchanged from run 32 (below): spot-check ramp post 1, optional human Reddit post, PR #59, WP
rotation / keyword tool / Vercel cleanup.

---

## Daily-ops routine (run 32)

**Advanced: backlog #21 — probe content-marker checks ported to Linux (PR #62, merged). Also:
runner collector pre-flight verified working on its first scheduled exercise (the 3-run collector
gap is closed), and the §7 posting ramp got a pacing rule + a ready-to-post Reddit draft.**

### Health & metrics (KR1-2, KR1-4)
- **Runner pre-flight WORKED** (first run after PR #61 + the host-side runner fix): the 00:30Z
  routine log shows `probe collected. / metrics collected.` — fresh artifact
  `site-check-20260713T003001Z.json` (`/`, `/listeners`, `/login` all **200**, overall pass) and
  today's KR1-2 rows appended by the runner itself, exactly what run 31 asked run 32 to verify.
  No in-session self-collection needed for the first time since run 28.
- Awareness A4=34 / A5=6 / pos 14.7 / CTR 17.6% / **0 non-branded of 5 queries** (28d window
  slid — the 3 posts are still awaiting index post-#22 requests); funnel F1=1, F2=5, F3=0, F4=0,
  R1=0 (Firebase Admin live reads, unchanged). A1/A2/A3 n/a (GA4 / WP analytics / GSC-UI unwired).

### What shipped (PR #62, non-§3b, self-merged per §3a)
- **`ops/check-site.mjs`** — repo-owned synthetic probe restoring the content-marker assertions
  the Windows `check-site.ps1` had and the Linux HTTP-200-only port lost: each critical path must
  return 200 **and** contain its marker (`/`→"DonaTalk", `/listeners`→"listener",
  `/login`→"DonaTalk"). Catches the 200-but-broken class (e.g. a Next.js error page served with
  200) that pure status checks miss. Same artifact shape (`timestamp`/`overall` are what
  `get-metrics.mjs` + `deploy-web.mjs` consume; `marker`/`marker_ok` additive), same
  `ALERT-check-site-*` + exit behavior.
- **`ops/lib/site-probe.mjs`** — pure check/artifact logic, **+9 unit tests (suite 28 files /
  378)**; tsc clean; live-verified against production (all 3 paths 200 + markers).
- **Host-side (`ops-shared/check-site.sh`, untracked shared infra):** donatalk branch now
  delegates to `$REPO/ops/check-site.mjs` when present (`timeout 300`, same pattern as TierUp's
  full probe), curl loop kept as fallback. In-session bash execution is permission-gated, so the
  delegation's first live exercise is the **run-33 cron pre-flight — verify its artifact contains
  `"markers":"checked"`**. `node`/`timeout` confirmed present under the script's restricted PATH.

### §7 ramp: post 2/5 deferred (pacing), Reddit draft ready instead
Post 1/5 went out 21:37Z; this run landed 3h later at ~7:30pm Saturday CT with the Board's
spot-check still pending. A second machine post that close, late-night, on the shared account
would read bot-like and outrun the ramp's calibration purpose — **deferred to a daytime run** and
recorded a durable pacing rule (DECISIONS 2026-07-13: ≤1 machine post per platform per day during
the ramp, daytime/early-evening CT, vary platform/content). Ramp advanced via the sanctioned
Reddit path instead — draft below.

#### Ready-to-post Reddit answer (human posts from No_Pangolin1040 — do not automate)
**Target:** an active r/sales (or r/startups) thread where the OP asks some form of "cold email
reply rates are terrible / what's actually working instead?" — pick a thread from the last week
with real engagement; post as a **comment**, adapt the first line to the OP's actual numbers.
Reddit rules: no link drop; DonaTalk named once, affiliation disclosed, only expand if asked.

> Reply rates aren't just "low," they've been sliding — platform averages went from ~5% in 2024
> to ~3.4% this year, and that's before Gmail/Microsoft tightened bulk-sender enforcement. The
> elite senders still pull 10%+, but they're doing something structurally different: fewer,
> costlier touches instead of more volume.
>
> What I'd actually try, in order:
>
> 1. **Map warm paths before writing another sequence.** A double-opt-in intro through a mutual
> converts at a completely different level than any cold template (studies put warm intros around
> 20–40% vs low single digits cold). An hour on LinkedIn mutuals usually beats a week of new copy.
>
> 2. **Make the first touch cost you something real.** Reference their actual work, record a
> 60-second video about *their* funnel, send real analysis. Now that AI can write infinite
> "personalized" emails, effort you can't fake is the only signal left that lands.
>
> 3. Full disclosure, this one's mine — I'm building **DonaTalk**, which takes the costly-signal
> idea literally: you commit a donation to a charity the person actually cares about in exchange
> for a real meeting. The bet is that a sincere, non-fakeable gesture beats another sequence, and
> the prospect's cause wins either way. Early days, so I won't pretend it's proven — happy to
> share what I've learned about the mechanic even if you never touch it.
>
> The common thread: the channel isn't dead, the free-signal era is.

*§6 check: ~5.1%→~3.4% + 10%+ elite + 20–40% warm-intro ranges all per the pinned convention
(`research/2026-07-10-keywords.md`); DonaTalk framed as unproven ("early days") because F3/F4=0 —
no traction claims; affiliation disclosed.* Once posted, log the URL in `backlink-ledger.md`
(row 2, nofollow) + write the `ops/logs/POST-reddit-*` §7 ramp log.

### Charter compliance
Ops tooling + docs only, non-§3b, PR #62 self-merged per §3a. No version bump (ops-only, run-26
precedent). Merge-to-main deploy handled per the deploy-flow rule: no CLI deploy, post-merge
health confirmed by the live probe run. **No machine posting this run** (§7 pacing) — Reddit
draft is human-gated by design. No secrets printed or committed.

### Cross-business learnings (for TierUp's routine)
- The **repo-owned-probe delegation pattern** is now symmetric: `check-site.sh` delegates to
  `<repo>/ops/check-site.mjs` for both businesses when present. DonaTalk's marker-check probe
  (`ops/lib/site-probe.mjs` + tests) is a ready template for any TierUp page-content assertions.
- **Verify runner fixes on their first scheduled firing**, not just by code review — run 31 wrote
  "verify run 32's log shows probe collected/metrics collected" into the brief, and that check is
  what confirmed the collector gap actually closed. TierUp's header fix (flagged run 31) still
  needs doing + the same next-run verification.

### Board asks
1. **Spot-check ramp post 1/5** (§7, unchanged): https://x.com/MichaelLeetj/status/2076329992749777091
   — log in `ops/logs/POST-x-20260712T213758Z.txt`. Post 2/5 waits for a daytime run.
2. **Human Reddit post** (optional, 5 min): the ready-to-post answer above — pick a live thread,
   adapt line 1, post from No_Pangolin1040.
3. **Review + merge PR #59** (nodemailer 9.0.3 — last remaining high vuln), unchanged.
4. Standing: WP password rotation; keyword-tool access; optional Vercel dashboard cleanup
   (`dpl_7U6T7Fp1nQPMv5JunC6Bz3iLtGU6`).
