# Daily report — 2026-07-13

## Daily-ops routine (run 33, 03:30Z)

**Advanced: backlog #24 — `/vs` curated-vs-self-serve category context shipped live (PR #63,
v0.20.0).** Also: run-32's two follow-up checks both passed (probe delegation + runner collection).

### Health & metrics (KR1-2, KR1-4)
- **Runner pre-flight green, 2nd consecutive run:** fresh `site-check-20260713T033002Z.json`
  (all 3 paths 200, overall pass) + today's KR1-2 rows appended by the runner. **The run-32
  verification ask is confirmed: the artifact contains `"markers":"checked"`** — the
  `check-site.sh` → `ops/check-site.mjs` delegation (PR #62) works under cron; content-marker
  assertions are now live in the scheduled probe path.
- Awareness A4=34 / A5=6 / pos 14.7 / CTR 17.6% / 0 non-branded of 5 queries; funnel F1=1, F2=5,
  F3=0, F4=0, R1=0 (unchanged from the 00:30Z row — same GSC/Firestore windows). A1/A2/A3 n/a
  (GA4 / WP analytics / GSC-UI unwired).

### What shipped (PR #63, v0.20.0, non-§3b, self-merged per §3a)
- `app/vs/page.tsx` — new **"Within the category: curated vs. self-serve"** section (two cards:
  curated executive marketplaces vs. self-serve DonaTalk) + matching FAQ entry (auto-included in
  the FAQPage JSON-LD) + two category keywords (`donation for a meeting`, `executive meetings for
  charity`). Closes item 24 (MeetMagic-aware refresh from the 07-12 competitor scan): the page now
  acknowledges the curated/enterprise form of the donation-for-meeting model honestly at category
  level — **no competitor named, no "first/only" claims (§6)** — and positions DonaTalk by real
  differentiators (self-serve, any vertical, pitcher-set donation from $10 to a listener-chosen
  cause, decline = no charge, 4.9% fee).
- Gates: tsc clean, 28 files / 378 tests pass; version 0.19.2 → 0.20.0 + CHANGELOG + ref docs.
- Deploy: merge-to-main = deploy (git integration, per the 07-12 deploy-flow rule); new section
  verified live on production post-merge; post-merge probe green.

### §7 ramp: post 2/5 still deferred (pacing)
This run fired 22:30 Saturday CT — same CT day as post 1 and late-night, so the 07-13 pacing rule
(≤1 machine post/platform/day, daytime CT) defers again. The first Sunday-daytime-CT run should
fire post 2/5 (vary content vs post 1; X is still the only machine-postable platform — LinkedIn
helper unbuilt, Reddit human-gated). Board spot-check of post 1 still pending.

### Board asks
Unchanged from run 32 (below): spot-check ramp post 1, optional human Reddit post, PR #59, WP
rotation / keyword tool / Vercel cleanup.

---

## Daily-ops routine (run 32)

**Advanced: backlog #21 — probe content-marker checks ported to Linux (PR #62, merged). Also:
runner collector pre-flight verified working on its first scheduled exercise (the 3-run collector
gap is closed), and the §7 posting ramp got a pacing rule + a ready-to-post Reddit draft.**

### Health & metrics (KR1-2, KR1-4)
- **Runner pre-flight WORKED** (first run after PR #61 + the host-side runner fix): the 00:30Z
  routine log shows `probe collected. / metrics collected.` — fresh artifact
  `site-check-20260713T003001Z.json` (`/`, `/listeners`, `/login` all **200**, overall pass) and
  today's KR1-2 rows appended by the runner itself, exactly what run 31 asked run 32 to verify.
  No in-session self-collection needed for the first time since run 28.
- Awareness A4=34 / A5=6 / pos 14.7 / CTR 17.6% / **0 non-branded of 5 queries** (28d window
  slid — the 3 posts are still awaiting index post-#22 requests); funnel F1=1, F2=5, F3=0, F4=0,
  R1=0 (Firebase Admin live reads, unchanged). A1/A2/A3 n/a (GA4 / WP analytics / GSC-UI unwired).

### What shipped (PR #62, non-§3b, self-merged per §3a)
- **`ops/check-site.mjs`** — repo-owned synthetic probe restoring the content-marker assertions
  the Windows `check-site.ps1` had and the Linux HTTP-200-only port lost: each critical path must
  return 200 **and** contain its marker (`/`→"DonaTalk", `/listeners`→"listener",
  `/login`→"DonaTalk"). Catches the 200-but-broken class (e.g. a Next.js error page served with
  200) that pure status checks miss. Same artifact shape (`timestamp`/`overall` are what
  `get-metrics.mjs` + `deploy-web.mjs` consume; `marker`/`marker_ok` additive), same
  `ALERT-check-site-*` + exit behavior.
- **`ops/lib/site-probe.mjs`** — pure check/artifact logic, **+9 unit tests (suite 28 files /
  378)**; tsc clean; live-verified against production (all 3 paths 200 + markers).
- **Host-side (`ops-shared/check-site.sh`, untracked shared infra):** donatalk branch now
  delegates to `$REPO/ops/check-site.mjs` when present (`timeout 300`, same pattern as TierUp's
  full probe), curl loop kept as fallback. In-session bash execution is permission-gated, so the
  delegation's first live exercise is the **run-33 cron pre-flight — verify its artifact contains
  `"markers":"checked"`**. `node`/`timeout` confirmed present under the script's restricted PATH.

### §7 ramp: post 2/5 deferred (pacing), Reddit draft ready instead
Post 1/5 went out 21:37Z; this run landed 3h later at ~7:30pm Saturday CT with the Board's
spot-check still pending. A second machine post that close, late-night, on the shared account
would read bot-like and outrun the ramp's calibration purpose — **deferred to a daytime run** and
recorded a durable pacing rule (DECISIONS 2026-07-13: ≤1 machine post per platform per day during
the ramp, daytime/early-evening CT, vary platform/content). Ramp advanced via the sanctioned
Reddit path instead — draft below.

#### Ready-to-post Reddit answer (human posts from No_Pangolin1040 — do not automate)
**Target:** an active r/sales (or r/startups) thread where the OP asks some form of "cold email
reply rates are terrible / what's actually working instead?" — pick a thread from the last week
with real engagement; post as a **comment**, adapt the first line to the OP's actual numbers.
Reddit rules: no link drop; DonaTalk named once, affiliation disclosed, only expand if asked.

> Reply rates aren't just "low," they've been sliding — platform averages went from ~5% in 2024
> to ~3.4% this year, and that's before Gmail/Microsoft tightened bulk-sender enforcement. The
> elite senders still pull 10%+, but they're doing something structurally different: fewer,
> costlier touches instead of more volume.
>
> What I'd actually try, in order:
>
> 1. **Map warm paths before writing another sequence.** A double-opt-in intro through a mutual
> converts at a completely different level than any cold template (studies put warm intros around
> 20–40% vs low single digits cold). An hour on LinkedIn mutuals usually beats a week of new copy.
>
> 2. **Make the first touch cost you something real.** Reference their actual work, record a
> 60-second video about *their* funnel, send real analysis. Now that AI can write infinite
> "personalized" emails, effort you can't fake is the only signal left that lands.
>
> 3. Full disclosure, this one's mine — I'm building **DonaTalk**, which takes the costly-signal
> idea literally: you commit a donation to a charity the person actually cares about in exchange
> for a real meeting. The bet is that a sincere, non-fakeable gesture beats another sequence, and
> the prospect's cause wins either way. Early days, so I won't pretend it's proven — happy to
> share what I've learned about the mechanic even if you never touch it.
>
> The common thread: the channel isn't dead, the free-signal era is.

*§6 check: ~5.1%→~3.4% + 10%+ elite + 20–40% warm-intro ranges all per the pinned convention
(`research/2026-07-10-keywords.md`); DonaTalk framed as unproven ("early days") because F3/F4=0 —
no traction claims; affiliation disclosed.* Once posted, log the URL in `backlink-ledger.md`
(row 2, nofollow) + write the `ops/logs/POST-reddit-*` §7 ramp log.

### Charter compliance
Ops tooling + docs only, non-§3b, PR #62 self-merged per §3a. No version bump (ops-only, run-26
precedent). Merge-to-main deploy handled per the deploy-flow rule: no CLI deploy, post-merge
health confirmed by the live probe run. **No machine posting this run** (§7 pacing) — Reddit
draft is human-gated by design. No secrets printed or committed.

### Cross-business learnings (for TierUp's routine)
- The **repo-owned-probe delegation pattern** is now symmetric: `check-site.sh` delegates to
  `<repo>/ops/check-site.mjs` for both businesses when present. DonaTalk's marker-check probe
  (`ops/lib/site-probe.mjs` + tests) is a ready template for any TierUp page-content assertions.
- **Verify runner fixes on their first scheduled firing**, not just by code review — run 31 wrote
  "verify run 32's log shows probe collected/metrics collected" into the brief, and that check is
  what confirmed the collector gap actually closed. TierUp's header fix (flagged run 31) still
  needs doing + the same next-run verification.

### Board asks
1. **Spot-check ramp post 1/5** (§7, unchanged): https://x.com/MichaelLeetj/status/2076329992749777091
   — log in `ops/logs/POST-x-20260712T213758Z.txt`. Post 2/5 waits for a daytime run.
2. **Human Reddit post** (optional, 5 min): the ready-to-post answer above — pick a live thread,
   adapt line 1, post from No_Pangolin1040.
3. **Review + merge PR #59** (nodemailer 9.0.3 — last remaining high vuln), unchanged.
4. Standing: WP password rotation; keyword-tool access; optional Vercel dashboard cleanup
   (`dpl_7U6T7Fp1nQPMv5JunC6Bz3iLtGU6`).
