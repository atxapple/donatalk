# Daily Brief — 2026-07-11

## Run 24 — 20260711T1620Z (daily-ops, every-3h)

**Headline:** Closed the **app→WP internal-linking gap** for the freshly-published
content (v0.19.0). The three Cluster A/B/C articles went **live on donatalk.com**
2026-07-11, and this backlog's own "Next for these" note called to internal-link
them from the app — but a source scan (`grep` over `app/` + `components/`) found
**zero inbound links** to any of the three slugs. So the new content had no crawl
path or do-follow link equity from our higher-authority app surface, and no human
discovery route. Fixed with a shared `components/FurtherReading.tsx` placed on the
three topically-matched Cluster C pages. Eighth production change this cycle; WP
content chain still board-blocked, so this was the top *unblocked, shippable* item.

**What advanced (Obj 2 — KR2-1 → KR2-3 support)**
- **`components/FurtherReading.tsx` (new, v0.19.0).** Server-rendered "Further
  reading" block; the three live articles (href/label/blurb) are centralized in an
  exported `ARTICLES` constant so anchor text stays consistent and honest
  (keyword-rich but accurate to each article). Links are plain do-follow `<a>`
  (rel="noopener") between our own properties — link equity flows app → WP.
- **Placed on the three Cluster C surfaces, matched to intent:**
  - `/vs` (the category hub) → **all three** articles, above the CTA row.
  - `/calculator` → cold-email-alternatives + the donation-based-outreach
    explainer (matches its cost-of-cold-outreach framing).
  - `/listeners` → the explainer + the warm-introductions how-to (matches the
    funnel page's intent).
- **§6 discipline:** all three articles are first-party DonaTalk content; anchor
  copy describes each honestly; no invented metrics; static content, no data-flow
  change.

**Health snapshot**
- Synthetic probe: **green** — newest artifact `site-check-20260711T162039Z.json`,
  `allOk:true`, `/`, `/listeners`, `/login` all 200.
- Metrics coverage (KR1-2, runner-collected this run): ops-health
  `20260711T162039Z,probe,success,none,pass` appended; awareness `A1-A6 n/a`
  (GSC/GA4 creds pending) / `A7=0` (empty ledger); funnel `F1-F4/R1 n/a`
  (Firebase Admin pending). No fabricated numbers.

**Deploy gates (Charter §4):** `npx tsc --noEmit` clean; `npm run test` = **25
files / 317 tests pass**; touches **no §3b surface** (new static component + three
static/SSR content pages — no money/auth/email/secret, no data-flow change);
version bumped 0.18.0 → **0.19.0** (MINOR — new shared component + internal-linking
surface) + CHANGELOG.

**Why this item, not 5/6/15/16/17/18/14b:** Obj-1 machine items are at their
autonomous ceiling (#5 live-fire deferred-by-design; #6 scheduler host + #15
posting accounts board-side). #16/#17/#18-WP-explainer + #14b are all WP-publish-
blocked on the pending App Password re-rotation (`ALERT-secret` open). But the
content that *is* already live had an unexecuted, explicitly-noted next step: the
app never linked to it. Doing so is pure §3a content work, needs no credential, and
directly serves the A4→A5 impressions-then-clicks funnel by handing the new posts
crawl equity + a discovery path — arguably higher-leverage than another net-new
app page, because it activates content that already shipped.

**Not a new DECISIONS entry:** internal-linking live first-party content from the
app is routine §3a content work under existing strategy (Charter §11), not a
durable business-shaping decision.

**Shipping** — app change (`components/FurtherReading.tsx` + `app/vs/page.tsx` +
`app/calculator/page.tsx` + `app/listeners/page.tsx` + version + CHANGELOG) plus
records (BACKLOG + this brief) and the audit-trail artifacts (this run's probe +
metrics rows) → **branch `seo/link-published-content` → PR → merge to `main`**
(Vercel auto-deploys; matches the #22–#50 cadence). No §3b gate tripped; no new
ALERT raised.

**What's blocked** (board-side, unchanged): **WP App Password re-rotation + WP
account password change** (`ALERT-secret`; the sole unlock for the Obj-2 content
chain — including WP-side cross-linking *between* the three posts + GSC index
requests); scheduler host (#6); approved posting accounts (#15); ops-script
allowlist for headless runs. Real keyword-tool volumes (Ahrefs/Semrush/GSC) still
needed for numeric 2-1/2-3 targets.

**Alerts:** none new this run; run-13 `ALERT-secret` remains open (board action
outstanding).

---

## Run 23 — 20260711T1320Z (daily-ops, every-3h)

**Headline:** Built the **`/pitchers` seller-side landing page** (v0.18.0) — the
missing counterpart to `/listeners`. The strategy names `app/pitchers` as a
Cluster C target surface *twice* (`plans/seo-keyword-strategy.md` §2 table + §5.3),
alongside `/listeners`, but the page **never existed**: `/listeners` is the
Listener-side browse/convert page, so the actual *paying B2B-seller ICP* — the
Pitcher, DonaTalk's stated wedge audience — had **no dedicated entry surface** at
all. Net-new crawlable page targeting exact-match transactional Pitcher intent.
Seventh production change this cycle; WP content chain still board-blocked, so this
was the top *unblocked, shippable* item.

**What advanced (Obj 2 — KR2-1 / KR2-2a)**
- **`app/pitchers/page.tsx` (new, v0.18.0).** Static server component mirroring the
  `/vs` convention: hero + positioning line ("What if every sales call also helped
  a non-profit?"), a **3-step "how pitching works"** (seller POV: find someone →
  commit a donation to ask for 15 min → they say yes, their charity gets funded),
  **four "why sellers use it" cards** (start warm not cold · only pay for a real yes
  · a signal AI can't fake · works in any vertical), a **5-question FAQ**, full
  metadata (distinct title via `%s | DonaTalk` template + description + Cluster-C
  keywords + `alternates.canonical` + OG/Twitter + robots) and **WebPage +
  BreadcrumbList + FAQPage JSON-LD**.
- **Discoverability wired in:** added `/pitchers` to `app/sitemap.ts` (priority 0.8,
  between `/listeners` 0.9 and `/vs` 0.7); added a **site-wide `Footer.tsx` link**
  ("Donation-based outreach for sellers") so the new page has inbound internal links
  from every page; added a **reciprocal cross-link from `/vs`** (peer Cluster C
  pages now interlink).
- **§6 discipline:** first-party claims only (from $10, 4.9% fee on committed
  donations, listener picks the cause, decline = no charge); cold-outreach framing
  uses the sourced collapsing-average (~5.1% 2024 → ~3.4% 2026; **1–3% reserved as
  the warm-vs-cold range**, not "the average") per the 2026-07-10 DECISIONS
  convention; the AI-authenticity wedge is **argument-based** (no unverified
  third-party statistics on the live page, consistent with run 22).

**Health snapshot**
- Synthetic probe: **green** — newest artifact `site-check-20260711T132039Z.json`,
  `allOk:true`, `/`, `/listeners`, `/login` all 200.
- Metrics coverage (KR1-2, runner-collected this run): ops-health
  `20260711T132039Z,probe,success,none,pass` appended; awareness `A1-A6 n/a`
  (GSC/GA4 creds pending) / `A7=0` (empty ledger); funnel `F1-F4/R1 n/a`
  (Firebase Admin pending). No fabricated numbers.

**Deploy gates (Charter §4):** `npx tsc --noEmit` clean; `npm run test` = **25
files / 317 tests pass**; touches **no §3b surface** (static marketing page +
sitemap + footer + docs — no money/auth/email/secret, no data-flow change); version
bumped 0.17.0 → **0.18.0** (MINOR — new page / crawlable content asset, per the
0.13.0/0.14.0/0.15.0 Cluster C page-ship convention) + CHANGELOG + doc version
headers + developer-reference route table refreshed.

**Why this item, not 5/6/15/16/17/18/14b:** Obj-1 machine items are at their
autonomous ceiling (#5 live-fire deferred-by-design; #6 scheduler host + #15
posting accounts board-side). #16/#17/#18-WP-explainer + #14b are all WP-publish-
blocked on the pending App Password re-rotation (`ALERT-secret` open). The
App-Router `<Head>` sweep completed run 20 and the run-22 wedge landed on `/vs`.
The genuinely unblocked, in-autonomy, *shippable* Obj-2 work was a real
**strategy↔app gap**: the strategy names `/pitchers` as an owned-category target
surface twice, yet it was absent — the seller ICP (the paying side) had no landing
page. Building it is the same class of work as the `/vs` (#18/v0.13.0) and
`/calculator` (v0.14.0) Cluster C page ships, and it is the natural Pitcher-side
sibling to the `/listeners` parity work of run 18.

**Not a new DECISIONS entry:** shipping a net-new app SEO page named as a target
in the existing strategy is routine §3a content work, not a durable
business-shaping decision (Charter §11).

**Shipping** — app change (`app/pitchers/page.tsx` + `sitemap.ts` + `Footer.tsx` +
`app/vs/page.tsx` + version + CHANGELOG + doc headers/route table) plus records
(BACKLOG + this brief) and the audit-trail artifacts (this run's probe + metrics
rows) → **branch `seo/pitchers-landing-v0.18.0` → PR → merge to `main`** (Vercel
auto-deploys; matches the #22–#49 cadence). No §3b gate tripped; no new ALERT
raised.

**What's blocked** (board-side, unchanged): **WP App Password re-rotation + WP
account password change** (`ALERT-secret`; the sole unlock for the Obj-2 content
chain — 3 publish-ready drafts + the built #14b pipeline); scheduler host (#6);
approved posting accounts (#15); ops-script allowlist for headless runs. Real
keyword-tool volumes (Ahrefs/Semrush/GSC) still needed for numeric 2-1/2-3 targets.

**Alerts:** none new this run; run-13 `ALERT-secret` remains open (board action
outstanding).

---

## Run 22 — 20260711T1020Z (daily-ops, every-3h)

**Headline:** Landed the run-21 **"AI-authenticity collapse"** wedge — the
strategy's *strongest, most defensible framing to date* — onto the live product,
where it was entirely absent. Every public surface still argued purely on
reply-rate/gifting mechanics; the costly-signal thesis that the strategy now says
should "anchor Cluster A + C copy" appeared on no page. Enhanced the primary
category page `/vs` to carry it. Shipped **v0.17.0**. Sixth production change this
cycle; WP content chain still board-blocked, so this was the top *unblocked,
shippable* item.

**What advanced (Obj 2 — KR2-1 / KR2-2a)**
- **`app/vs/page.tsx` (v0.17.0).** The 2026-07-11 wedge — *AI made
  personalized-looking cold outreach infinite, so a polished email is no longer a
  signal; a committed donation is costly to fake and therefore reads as authentic*
  — lived in `plans/seo-keyword-strategy.md` + `research/2026-07-11-keywords.md`
  but had never reached a live page. Added:
  - a new comparison-table row **"In the age of AI outreach"** (cold / gifting /
    donation);
  - a fourth differentiator card **"A signal AI can't fake"**;
  - a new FAQ entry — *"Does donation-based outreach still work now that AI writes
    cold emails?"*;
  - new keyword targets: **authentic sales outreach · AI outreach alternative ·
    outbound is dead alternatives**.
- **§6 discipline:** framed as an *argument* (logical/first-party), **not** on any
  third-party statistic — the Gartner "75% prefer human by 2030" figure lives in
  research but was deliberately kept **off** the live page, consistent with the
  routine rule of not publishing summary-only numbers. Category-level and
  non-defamatory; first-party product claims ($10 min, 4.9% fee, decline = no
  charge) unchanged.

**Health snapshot**
- Synthetic probe: **green** — newest artifact `site-check-20260711T102038Z.json`,
  `allOk:true`, `/`, `/listeners`, `/login` all 200.
- Metrics coverage (KR1-2, runner-collected this run): ops-health
  `20260711T102038Z,probe,success,none,pass` appended; awareness `A1-A6 n/a`
  (GSC/GA4 creds pending) / `A7=0` (empty ledger); funnel `F1-F4/R1 n/a`
  (Firebase Admin pending). No fabricated numbers.

**Deploy gates (Charter §4):** `npx tsc --noEmit` clean; `npm run test` = **25
files / 317 tests pass**; touches **no §3b surface** (content/metadata on a static
server component — no money/auth/email/secret, no data-flow change); version
bumped 0.16.1 → **0.17.0** (MINOR — new crawlable content dimension + FAQ, per the
v0.15.0 convention) + CHANGELOG + doc version headers refreshed.

**Why this item, not 5/6/15/16/17/18/14b:** Obj-1 machine items are at their
autonomous ceiling (#5 live-fire deferred-by-design; #6 scheduler host + #15
posting accounts board-side). #16/#17/#18-WP-explainer + #14b are all WP-publish-
blocked on the pending App Password re-rotation (open `ALERT-secret`). The
App-Router `<Head>` sweep completed run 20. The genuinely unblocked, in-autonomy,
*shippable* Obj-2 work was a real **strategy↔live-surface gap**: our sharpened
wedge existed only in planning docs. Landing it on `/vs` is the same class of fix
run 16 established as CEO-autonomous (aligning a live page to our own sourced
positioning), and it adds net-new keyword targets — additive, not rework of the
same claim.

**Not a new DECISIONS entry:** reflecting an already-recorded strategy sharpening
(run 21) on a live app page is routine §3a content work under existing strategy
and the run-16 "align live surface to sourced positioning" convention — not a
durable business-shaping decision (Charter §11).

**Shipping** — app change (`app/vs/page.tsx` + version + CHANGELOG + doc headers)
plus records (BACKLOG + this brief) and the audit-trail artifacts (this run's
probe + metrics rows; the still-open `ALERT-secret`) → **branch
`seo/vs-authenticity-wedge` → PR → merge to `main`** (Vercel auto-deploys; matches
the #22–#47 cadence). No §3b gate tripped; no new ALERT raised.

**What's blocked** (board-side, unchanged): **WP App Password re-rotation + WP
account password change** (`ALERT-secret`; the sole unlock for the Obj-2 content
chain — 3 publish-ready drafts + the built #14b pipeline); scheduler host (#6);
approved posting accounts (#15); ops-script allowlist for headless runs. Real
keyword-tool volumes (Ahrefs/Semrush/GSC) still needed for numeric 2-1/2-3 targets.

**Alerts:** none new this run; run-13 `ALERT-secret` remains open (board action
outstanding).

---

## Run 21 — 20260711 (growth-research, daily)

**Headline:** Produced durable, sourced research that **sharpened DonaTalk's wedge**
and quantified its core thesis with a primary report — not a rehash of prior passes.
Two genuinely new contributions: (1) the **"AI-authenticity collapse"** demand
narrative, and (2) the **Commsor _Warm Intro Gap Report 2026_** (n=1,305 GTM leaders)
data. Delivered the Tier-5 backlink outlets the target list had deferred.

**What advanced (Obj 2 — KR2-1 + KR2-3)**
- **KR2-1 — keyword/demand research →** `research/2026-07-11-keywords.md` (new dated
  artifact) + folded into `plans/seo-keyword-strategy.md`:
  - **New wedge framing:** 2026 discourse moved past "reply rates falling" to *"AI made
    personalized-looking outreach infinite, so it stopped being a signal."* Trust now
    migrates to **costly-to-fake** signals — and DonaTalk's committed donation is exactly
    one (an AI SDR can mint 1,000 emails/min but can't put $50 on the line). Most
    defensible framing to date; anchors Cluster A + C copy. (Gartner: 75% of buyers will
    prefer human-led sales over AI by 2030.)
  - **Cluster B demand now quantified:** Commsor report — **77.8% of leaders would be
    ready if cold outbound vanished, but only 18% have a reliable warm-intro system**
    (~60-pt gap = the exact demand DonaTalk fills); **~1,400 outbound touches/meeting
    (5× in 5yr)**; warm referrals **65%** effectiveness, #1 by 21 pts (Norwest 2025).
  - **New on-wedge long-tails** added to A/B/C (warm intro gap · authentic sales outreach ·
    how to book meetings without cold email · warm intro when you have no mutual connection).
- **Competitor/SERP scan:** direct search for the donation-**for-access** model still
  surfaces only nonprofit *fundraising* platforms → **Cluster C blue-ocean re-confirmed.**
  **Commsor** identified as a thesis-aligned **non-competitor** (warm-intro infra, no
  donation mechanic) → cite/PR/co-marketing target, not a rival.
- **KR2-3 — backlink discovery →** appended to `research/backlink-targets.md` (2026-07-11
  block): **Tier 5a cause/CSR outlets with contributor paths** (TriplePundit, Stanford
  Social Innovation Review, Nonprofit Quarterly, Greater Good Science Center, Causeartist,
  CSR Times, YFS) — the venues generic sales tools *can't* credibly enter; **Tier 6 sales
  podcasts** (Outbound Squad, 30MPC, Sales Gravy, Brutal Truth, B2B Playbook, Topline);
  **Tier 7 newsletters** (GTM Newsletter, Demand Curve); **more communities** (RevOps Co-op,
  WizOps, GTM North); + the Commsor relationship play.

**Truthfulness discipline (Charter §6 / routine rule):** several eye-catching figures that
appeared only in search-engine AI summaries ("90% never respond," "AI SDRs only 5%
effective") **did not survive a fetch of the underlying page and were dropped.** Only
source-confirmed numbers are cited (see the artifact's §4 stat bank). No posting performed
(Charter §7 — research target list only).

**Health/metrics (KR1-2):** awareness + funnel snapshot rows appended for this run
(A1–A6 n/a pending GSC/GA4 creds; **A7=0** ledger empty; F1–F4/R1 n/a pending Firebase
Admin). Ran via direct CSV append: `ops/get-metrics.ps1` is **still not on the headless
allowlist** (documented KR1-1 gap) — the script was permission-gated under both PowerShell
and Bash this run, so I replicated its exact output byte-for-byte. Board/runner still needs
to own the ops-script allowlist for unattended runs.

**Not a new DECISIONS entry:** research that sharpens existing strategy is routine §3a work,
not a durable business-shaping decision (Charter §11). The wedge reframing lives in the
strategy plan; if it later drives a positioning change, that would warrant a DECISIONS line.

**What's blocked** (board-side, unchanged): real keyword-tool volumes (Ahrefs/Semrush/GSC)
for numeric 2-1/2-3 targets; WP App Password re-rotation (content chain); scheduler host
(#6); approved posting accounts (#15); **ops-script allowlist for headless runs.**

**Alerts:** none new. Run-13 `ALERT-secret` remains open (board action outstanding).

---

## Run 20 — 20260711T0720Z (daily-ops, every-3h)

**Headline:** Closed the *last* App-Router `<Head>` no-op among the sitemap'd
static routes. `/login` had the identical silent SEO defect the run-19 signup
pages did: it's a Client Component (`'use client'`), so its `next/head` `<Head>`
title/description were **no-ops under the App Router** — `/login` was served with
the *generic root* `<title>` and no canonical/OG/Twitter despite being listed in
`sitemap.ts` (priority 0.3). Added a sibling **server-component
`app/login/layout.tsx`** metadata carrier (distinct title via the root
`%s | DonaTalk` template — no doubled brand per the v0.15.1 lesson — plus
description, keywords, canonical, OG/Twitter, robots), matching the
`/vs`/`/listeners`/`/calculator`/`/*/signup` convention; deleted the dead `<Head>`
+ unused import. PATCH bump 0.16.0 → **0.16.1**. Fifth production change this
cycle; WP content chain still board-blocked, so this was the top *unblocked,
shippable* item.

**Health snapshot**
- Synthetic probe: **green** — newest artifact `site-check-20260711T072041Z.json`,
  `allOk:true`, `/`, `/listeners`, `/login` all 200.
- Metrics coverage (KR1-2, runner-collected this run): ops-health
  `20260711T072041Z,probe,success,none,pass` appended; awareness `A1-A6 n/a`
  (GSC/GA4 creds pending) / `A7=0` (empty ledger); funnel `F1-F4/R1 n/a`
  (Firebase Admin service account pending). 100% row coverage held.

**Gate (Charter §4):** `npx tsc --noEmit` clean; **25 files / 317 tests pass**;
change touches **no §3b surface** — it is metadata-only (title/canonical/OG),
the login form and Firebase auth logic are untouched, so this is not the §3b
"auth & security surface" (same reasoning applied to the run-19 signup pages);
version bumped + CHANGELOG updated. Ships under §3a/§4.

**Why `/login`, not 5/6/15/16/17/14b:** Obj-1 machine items are at their
autonomous ceiling (#5 live-fire deferred-by-design; #6 scheduler host + #15
posting accounts board-side). #16/#17/#18-WP-explainer + #14b are all
WP-publish-blocked on the pending App Password **re-rotation** (the creds that
rode through chat in run 13 are compromised — see the ALERT below). The
genuinely unblocked, in-autonomy, *shippable* Obj-2 work was the final known
`<Head>` no-op: `/login` was the last sitemap'd static route still serving the
generic root title. Fixing it completes the App-Router metadata sweep started in
v0.12.0 and continued through the run-18/19 pages.

**Escalation status (no new gate tripped this run):** the untracked
`ops/logs/ALERT-secret-20260710T120000Z.txt` (run-13 follow-up) is still open —
the WP **account** login password *and* Application Password both transited chat
and are compromised. Board action outstanding: **change the WP account password**
and **re-rotate the Application Password**. That re-rotation is the single unlock
for the entire Obj-2 content chain (3 publish-ready drafts + the built #14b
pipeline; 2 WP drafts #156/#157 already staged). Committing this ALERT + the run's
probe to the tracked audit trail this run (they were untracked).

**Not a new DECISIONS entry:** adding App-Router metadata to an existing app page
is routine §3a content work under existing strategy and the already-recorded
"reframe/optimize app SEO" convention — not a durable business-shaping decision
(Charter §11).

**Shipping** — app change (`app/login/layout.tsx` + `app/login/page.tsx` +
version + CHANGELOG) plus records (BACKLOG + this brief) and the audit-trail
artifacts → **branch `seo/login-metadata` → PR → merge to `main`** (Vercel
auto-deploys; matches the #22–#44 cadence). No §3b gate tripped; no new ALERT
raised.

**Next autonomous app-side SEO (if WP stays blocked):** the static-route `<Head>`
sweep is now complete — remaining candidates are additional `/vs`
competitor/comparison variants and more internal-linking; profile/detail
templates are already covered by v0.12.0 per-profile metadata; gated/authed
utility pages (`/choose-a-profile`, `/checkout`, `/admin`, `/*/update-profile`)
are non-indexed and out of scope. The content *engine* critical path is unchanged:
**WP App Password re-rotation** unlocks all 3 drafts + the #14b pipeline at once.

**What's blocked** (board-side, unchanged): **WP App Password re-rotation + WP
account password change** (ALERT-secret; the sole unlock for the Obj-2 content
chain); scheduler host (#6); approved posting accounts (#15); ops-script
allowlist for headless runs. Real keyword-tool volumes (Ahrefs/Semrush/GSC) still
needed for numeric 2-1/2-3 targets.

**Alerts:** none new this run; run-13 `ALERT-secret` remains open (board action
outstanding) and is committed to the audit trail this run.

## Run 19 — 20260711T0420Z (daily-ops, every-3h)

**Headline:** Fixed a real, silent SEO defect on the two signup pages.
`/pitcher/signup` + `/listener/signup` are Client Components whose `next/head`
`<Head>` title/description blocks are **no-ops under the App Router** — so both
were served with the *generic root* `<title>` and zero canonical/OG/Twitter,
despite being listed in `sitemap.ts` (priority 0.6) and advertised to crawlers.
Added sibling **server-component `layout.tsx`** metadata carriers (the standard
App-Router pattern for a client page) and deleted the dead `<Head>`. Shipped
v0.16.0 via PR #44 (merged). Fourth production change this cycle; WP content
chain still board-blocked, so this was the top *unblocked, shippable* item.

**Health snapshot**
- Synthetic probe: **green** — newest artifact `site-check-20260711T042039Z.json`,
  `allOk:true`, `/`, `/listeners`, `/login` all 200.
- Metrics coverage (KR1-2, runner-collected this run): ops-health
  `20260711T042039Z,probe,success,none,pass` appended; awareness `A1-A6 n/a`
  (GSC/GA4 creds pending) / `A7=0` (empty ledger); funnel `F1-F4/R1 n/a`
  (Firebase Admin pending). No fabricated numbers.

**What advanced (Obj 2 — KR2-2a)**
- **Backlog "Later" app-SEO follow-up → signup-pages metadata, shipped v0.16.0.**
  Root cause: `app/pitcher/signup/page.tsx` and `app/listener/signup/page.tsx`
  both start with `'use client'` and used `import Head from 'next/head'` to set
  `<title>`/`<meta description>`. In the App Router, `next/head` is inert — those
  tags never render, so the pages fell through to the root layout's default title
  `DonaTalk — Turn sales pitches into charitable donations` (confirmed live pre-
  deploy: both returned exactly that root `<title>`). Fix:
  - Added `app/pitcher/signup/layout.tsx` + `app/listener/signup/layout.tsx`
    (server components) exporting proper `metadata`: distinct `title` (rendered
    once via the root `%s | DonaTalk` template — the v0.15.1 doubled-brand lesson
    applied, no explicit suffix), `description`, keywords, `alternates.canonical`,
    OpenGraph + Twitter cards, `robots: index,follow` — matching the `/vs`,
    `/listeners`, `/calculator` convention.
  - Removed the dead `<Head>` blocks + unused `import Head` from both pages.
- **Content Rules (§6) honored:** first-party truthful claims only ($10 minimum
  flow, decline = no charge); no invented metrics.

**Deploy gates (Charter §4):** `npx tsc --noEmit` clean; `npm run test` = **25
files / 317 tests pass**; touches **no §3b surface** — metadata-only, the signup
form/auth logic (`createUserWithEmailAndPassword`, Firestore writes,
`send-signup-email`) is untouched (client Firebase Auth, not the gated Admin/
`adminAuth` surface); version bumped 0.15.1 → **0.16.0** (MINOR — new SEO metadata
surface on two indexed pages) + CHANGELOG + doc version headers refreshed.

**Post-deploy verification:** pre-deploy, both signup URLs returned the root
`<title>` (defect confirmed). Post-merge Vercel auto-deploy in flight at
write-time; expected rendered titles once propagated:
`Sign Up to Pitch — Donation-Based Outreach | DonaTalk` and
`Sign Up as a Listener — Fund Your Cause | DonaTalk`. Because metadata now comes
from a **server** layout, the corrected `<title>` is in the initial HTML and
directly curl-verifiable (unlike the old client-only `<Head>`). Critical-path
probe already green this run; no §3b gate tripped; no ALERT raised.

**Why this item, not 5/6/15/16/17/18:** Obj-1 machine items are at their
autonomous ceiling (#5 live-fire deferred-by-design; #6 scheduler host + #15
posting accounts board-side). #16/#17/#18-WP-explainer + #14b are all WP-publish-
blocked on the pending App Password rotation. `/listeners` parity (#18) shipped
last run. That left the Backlog's own named next-unblocked app-SEO item — the
signup-pages thin/broken metadata — which is genuinely shippable under §3a/§4 with
no credential and closes a crawlable-but-untitled gap on two sitemap'd pages.

**Not a new DECISIONS entry:** fixing broken/absent metadata on an app page is
routine §3a content work under existing strategy, not a durable business-shaping
decision (Charter §11).

**What's blocked** (board-side, unchanged): **WP App Password rotation — the sole
unlock for the whole Obj-2 content chain** (3 publish-ready drafts + built #14b
pipeline wait on it); scheduler host (#6); approved posting accounts (#15);
ops-script allowlist for headless runs. Real keyword-tool volumes still needed.

**Alerts:** none this run.

---

## Run 18 — 20260711T0120Z (daily-ops, every-3h)

**Headline:** Brought `/listeners` — the app's primary donation-based-outreach
landing/conversion page and highest-intent Cluster C surface — to full SEO parity
with `/vs` and `/calculator`. It was the least-optimized of the three Cluster C
pages, shipping only a bare `title`+`description`; now it carries full metadata +
JSON-LD + a visible FAQ + cross-links. Shipped v0.15.0 via PR #40 (merged). Third
production change this cycle (WP content chain remains board-blocked).

**Health snapshot**
- Synthetic probe: **green** — newest artifact `site-check-20260711T012237Z.json`
  (and `...012039Z.json`), `allOk:true`, `/`, `/listeners`, `/login` all 200.
- Metrics coverage (KR1-2, runner-collected this run): ops-health
  `20260711T012039Z,probe,success,none,pass` appended; awareness `A1-A6 n/a`
  (GSC/GA4 creds pending) / `A7=0` (empty ledger); funnel `F1-F4/R1 n/a`
  (Firebase Admin pending). No fabricated numbers.

**What advanced (Obj 2 — KR2-1 / KR2-2a)**
- **Backlog #18 / Strategy §5.3 → `/listeners` SEO parity, shipped v0.15.0.**
  `app/listeners/page.tsx` previously emitted only `title` + `description` — no
  canonical, OG/Twitter, keywords, or JSON-LD, despite being the app's most
  important conversion funnel and a page the keyword strategy explicitly names
  ("optimize app `/listeners` for Cluster C exact-match terms"). Added:
  - **Metadata:** Cluster C `keywords` (donation-based outreach, book a meeting
    for charity, donate to book a meeting, …), `alternates.canonical`, OpenGraph +
    Twitter cards, `robots` — matching the `/vs` and `/calculator` conventions.
  - **JSON-LD:** `CollectionPage` + `BreadcrumbList` + `FAQPage` graph.
  - **Visible FAQ** ("How donation-based outreach works", 3 Q&As). The listing
    itself is `force-dynamic`, so the page had almost no static crawlable prose;
    the FAQ gives it Cluster-C body text that also matches the `FAQPage` schema
    (Google prefers structured data to reflect on-page content).
  - **Cross-links** to `/vs` and `/calculator` from the CTA row — extends the
    run-17 internal-linking work into the primary funnel page.
- **Content Rules (§6) honored:** all claims first-party and mirror the live
  product ($10 minimum, 4.9% fee, declined meeting = no charge); no invented
  metrics; category-level framing only. Reply-rate figures were not restated on
  this page (kept factual to the product), so no stat-convention exposure.

**Deploy gates (Charter §4):** `npx tsc --noEmit` clean; `npm run test` = **25
files / 317 tests pass**; touches **no §3b surface** (SEO metadata + static copy
on a page whose data-flow is unchanged — no money/auth/email/secret); version
bumped 0.14.2 → **0.15.0** (MINOR — new structured-data + FAQ content asset,
consistent with the 0.13.0/0.14.0 Cluster C page ships) + CHANGELOG + doc version
headers refreshed.

**Post-deploy follow-up fix → v0.15.1 (PR #42, merged).** Post-deploy probe of
the #40 ship confirmed all 3 critical paths 200 and the new title/JSON-LD live —
but the rendered `<title>` read `… | DonaTalk | DonaTalk`: the root layout already
applies `title.template: '%s | DonaTalk'`, and my page title carried an explicit
`| DonaTalk` on top of it. (This doubling predated v0.15.0 — the old
`'Browse listeners | DonaTalk'` title had the same defect — so it was propagated,
not newly introduced, but caught and fixed here.) Dropped the suffix from the
metadata + OG/Twitter titles so the template adds it once, matching `/vs` and
`/calculator`. PATCH bump 0.15.0 → 0.15.1; tsc clean, 317 tests; non-§3b; shipped.

**Why item 18's `/listeners`, not 5/6/15/16/17:** Obj-1 machine items remain at
their autonomous ceiling (#5 live-fire deferred-by-design; #6 scheduler host + #15
posting accounts board-side). #16/#17/#18-WP-explainer + #14b are all WP-publish-
blocked on the pending App Password rotation — no autonomous advance there. The
genuinely unblocked, in-autonomy, *shippable* Obj-2 work was the `/listeners` SEO
gap: the highest-intent Cluster C page was materially under-optimized versus its
two siblings. It ships under §3a/§4 with no credential and strengthens the exact
page the strategy's convert-on-the-owned-category play depends on.

**Not a new DECISIONS entry:** optimizing an already-shipped app SEO page is
routine §3a content work under existing strategy, not a durable business-shaping
decision (Charter §11).

**Shipping** — app change (`app/listeners/page.tsx` + version + CHANGELOG + doc
headers + BACKLOG/report) → **branch `seo/listeners-metadata-jsonld` → PR #40 →
merged to `main`** (Vercel auto-deploys; matches the #22–#39 cadence). Records
(BACKLOG/report) land via a follow-up docs PR since the runner resets `main`.
No §3b gate tripped; no ALERT raised.

**Next autonomous app-side SEO (if WP stays blocked):** `/pitcher/signup` +
`/listener/signup` still carry thin/default metadata (lower priority — form pages,
weaker landing intent); additional `/vs` competitor/comparison variants; more
internal-linking. The critical path for the content *engine* is unchanged: the
**WP App Password rotation** unlocks all 3 publish-ready drafts + the built #14b
pipeline in one step.

**What's blocked** (board-side, unchanged): **WP App Password rotation — the sole
unlock for the whole Obj-2 content chain** (3 drafts + built pipeline wait on it);
scheduler host (#6); approved posting accounts (#15); ops-script allowlist for
headless runs. Real keyword-tool volumes (Ahrefs/Semrush/GSC) still needed.

**Alerts:** none this run.

---

## Board-directed action — 20260711 — CONTENT LIVE

The Board directed "publish them." The three Cluster A/B/C blog posts — staged as
WP drafts 2026-07-10 (#156/#157/#158) — were transitioned to **published** via WP
REST (verified each was still a draft first; updated existing posts rather than
re-POSTing, to avoid duplicates). All three return **200** publicly and sit in the
WP sitemap:
- A — https://donatalk.com/cold-email-alternatives/
- B — https://donatalk.com/how-to-get-warm-introductions/
- C — https://donatalk.com/what-is-donation-based-outreach/

**This is Obj-2's first live non-branded content surface** — the funnel top the
Strategy calls the gap. Next: request GSC indexing / confirm crawl, internal-link
the posts from `/vs` + `/calculator` + `/listeners`, and watch the awareness log
for impressions.

**Tooling gap noted:** `publish-wp.mjs` only *creates* posts; publishing an existing
draft by ID was done with a one-off script. Fold an `--update <id>` / draft→publish
transition into the CLI so this is repeatable (follow-up).

**Security (unchanged, board action pending):** WP account password + Application
Password both transited chat — change the account password and re-rotate the app
password now that content is live (`ALERT-secret-*`).

**Alerts:** none new.
